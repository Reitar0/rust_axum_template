-- Начальная миграция.
-- Реальные таблицы добавятся вместе с фичами (вертикальными срезами).
-- Пока создаём безобидную служебную таблицу, чтобы миграция была непустой
-- и механизм миграций был сразу «живым».

CREATE TABLE IF NOT EXISTS _app_meta (
    key   TEXT PRIMARY KEY,
    value TEXT NOT NULL
);
